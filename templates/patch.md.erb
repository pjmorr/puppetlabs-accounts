- Picked into respective `stable` branches. [`Picked into Stable` <%= version.to_minor %> merged merge requests]:
  - [ ] REFERENCE_TO_MR_TO_PICK
- [ ] Check [`<group>/<project>/<%= version.stable_branch %>` CHANGELOG][stable-changelog]
- [ ] If required LINK_TO_CHANGE_CONTROL
- [ ] This release is also tracked as [JIRA version], the latest issue list for the  project is [LINK-TO-JIRA-PROJECT]
- [ ] Merge `<group>/<project>/<%= version.stable_branch %>` into the production branch: `<group>/<project><%= version.stable_branch(ee: true) %>`
- While waiting for tests to be green, now is a good time to start on the hipchat or confluence update.
- [ ] Ensure tests are green on Production: https://gitlab.com/gitlab-org/gitlab-ce/commits/<%= version.stable_branch %>
- [ ] Ensure tests are green on Master: https://gitlab.com/gitlab-org/gitlab-ee/commits/<%= version.stable_branch(ee: true) %>
- [ ] Check for any migrations in Production (and paste the diff below): `git diff <%= version.previous_tag %>..<%= version.stable_branch %> -- db/`
- [ ] Check for any migrations in Master (and paste the diff below): `git diff <%= version.previous_tag(ee: true) %>..<%= version.stable_branch(ee: true) %> -- db/`
- [ ] Tag and build (this is CI-automated) the `<%= version.to_patch %>` version using the [`release` task]:

      ```sh
      bundle exec rake "release[<%= version.to_patch %>]"
      ```
- [ ] Add patch notice to [<%= regression_issue.title %>](<%= regression_issue.url %>):

      ```
      `<%= version %>` has been tagged, further fixes will go into `<%= version.next_patch %>` as necessary.
      ```
- [ ] Check that [DEV packages are built] and appear under [`<group>/<project>-dev`] on [gitlab.dev.uberops.net]
- [ ] Deploy `<%= version %>` to [docs.gitlab.ubermonitoring.com]
- [ ] Deploy `<%= version %>` to [gitlab.dev.uberops.net]
- [ ] Create the `<%= version %>` version on https://version.gitlab.com
- [ ] Blog Post:
  - BLOG_POST_MR
  - BLOG_POST_URL
- [ ] Tweet (prepare the Tweet text below or paste the tweet URL instead):

      ```
      <project> <%= version %> is released! BLOG_POST_URL DESCRIPTION OF THE CHANGES
      ```
- [ ] Add [`dev/<%= version.tag %>` CHANGELOG][dev-tag-changelog] items to [`dev/master` CHANGELOG][dev-master-changelog]
- [ ] Create the next patch issue (if needed) using the [`patch_issue` task] (and put a reference to it below):

      ```sh
      bundle exec rake "patch_issue[<%= version.next_patch %>]"
      ```

---

For references:
- https://gitlab.ubermonitoring.com/<group>/<project>-prod/commits/<%= version.stable_branch(ee: true) %>
- https://gitlab.dev.uberops.net/<group>/<project>-dev/commits/<%= version.stable_branch %>

[`Picked into Stable` <%= version.to_minor %> merged merge requests]: https://dev.gitlab.ubermonitoring.net/<group>/<project>/merge_requests?label_name%5B%5D=Pick+into+Stable&milestone_title=<%= version.milestone_name %>&scope=all&sort=id_desc&state=merged

[stable-changelog]: https://dev.gitlab.ubermonitoring.net/<group>/<project>/blob/<%= version.stable_branch %>/CHANGELOG

[tag-changelog]: https://dev.gitlab.ubermonitoring.net/<group>/<project>/blob/<%= version.tag %>/CHANGELOG

[dev-master-changelog]: https://dev.gitlab.ubermonitoring.net/<group>/<project>/blob/master/CHANGELOG

[packages are built]: https://dev.gitlab.ubermonitoring.net/<group>/<project>/commits/<%= version.stable_branch %>
[`<group>/<project>-dev`]
[`YOUR_GROUP/YOUR_PROJECT`]: https://gitlab.ubermonitoring.com/<group>/<project>
[gitlab.dev.uberops.net]: https://gitlab.dev.uberops.net/<group>/<project>
[`release` task]: https://gitlab.ubermonitoring.com/uberops/release-tools/blob/master/doc%2Frake-tasks.md#releaseversion
[`patch_issue` task]: https://gitlab.ubermonitoring.com/uberops/release-tools/blob/master/doc%2Frake-tasks.md#patch_issueversion

[docs.pages.gitlab.ubermonitoring.com]: https://docs.pages.gilab.ubermonitoring.com/docs-repo/blob/master/staging.md
